# Secure Gateway Studio — Backend (Deprecated)

> [!WARNING]
> **This backend component is DEPRECATED.**
>
> Active development has transitioned exclusively to the **Chrome Extension** runtime located at [`../extension/`](../extension/).
> This loopback FastAPI backend is retained only as a historical reference implementation.

---

### Legacy Overview
The legacy loopback FastAPI app was the initial reference runtime for Secure Gateway Studio. For all active development, use the Chrome Extension.
