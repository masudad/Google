# Secure Gateway Studio — Frontend (Deprecated)

> [!WARNING]
> **This web frontend component is DEPRECATED.**
>
> Active development has transitioned exclusively to the **Chrome Extension** runtime located at [`../extension/`](../extension/).
> This React/Vite loopback UI is retained only as a historical reference implementation.

---

### Legacy Overview
The legacy web frontend was designed to run alongside the loopback FastAPI backend. For all active development and browser management features, refer to the Chrome Extension.
